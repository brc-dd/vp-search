{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM for the `page.evaluate` callbacks, which run in the browser
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["tests", "setup", "site", "site-hmr"]
}
